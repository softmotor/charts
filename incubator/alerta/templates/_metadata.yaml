{{- /*
alerta.metadata creates a standard metadata header.
It creates a 'metadata:' section with name and labels.
*/ -}}
{{ define "alerta.metadata" -}}
metadata:
  name: {{ template "alerta.fullname" . }}
  labels:
{{ include "alerta.labels.standard" . | indent 4 -}}
{{- end -}}
